# sourcehut, not GitHub. TRACK=git-tag uses `git ls-remote --tags`, which needs
# no API and no token and works against any git host -- the right default for
# anything that is not on GitHub.
TRACK=git-tag
GIT_URL=https://git.sr.ht/~proycon/wvkbd
CURRENT=0.20
VERSION_FROM=upstream-tag
